# Vídeos originais (material-fonte — são vários GB, não versionar)
Drone*/
Ambientes*/
*.MP4
*.MOV
*.mp4
*.mov

# macOS
.DS_Store

# Temporários de processamento de mídia
_work/
_logos/

# Book/folder PDF (material-fonte das logos — pesado)
*.pdf

# Config local do Claude Code (específica da máquina)
.claude/settings.local.json
_vista/
_sea/

# Foto-fonte solta na raiz (já otimizada em imagens/)
/delmandono.jpg

/Ativo 11.png

# vídeos otimizados da LP (devem ir pro repo)
!imagens/*.mp4

# Fotos-fonte da área de lazer (originais ~6MB — já otimizadas em imagens/lazer-*.jpg)
DSC*.jpg
# Foto-fonte dos donos (já otimizada em imagens/construtora-delman.jpg)
/delman-donos.png
# Pastas-fonte das marcas (já processadas em imagens/logo-*.png)
DELMAN E KAIRÓS*/
/Ativo 12.png
